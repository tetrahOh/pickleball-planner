import React, { useEffect, useMemo, useState } from 'react';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || 'https://uiopgjahnzzabsfdfcts.supabase.co';
const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || 'sb_publishable_Lq8i9SaaaklE5V6HF0PPCA_E752z49G';
const CURRENT_USER_KEY = 'family-pickleball-current-user';
const DEFAULT_NOTES = 'Bring your own paddle, towel, and water. Stretch beforehand.';
const LOCATION_DETAILS = {
  'Olympic Park Sports Hall': 'Grand Parade, Sydney Olympic Park NSW 2127',
  'Tennis World North Ryde': '16-18 Epping Rd, North Ryde NSW 2113'
};
const LOCATION_OPTIONS = Object.keys(LOCATION_DETAILS);
const DEFAULT_MAX_PLAYERS_PER_COURT = 4;
const blankSession = { title: '', date: '', startTime: '', durationHours: 1, location: '', address: '', courts: 1, courtNumbers: '', maxPlayersPerCourt: DEFAULT_MAX_PLAYERS_PER_COURT, costPerCourtHour: 0, isBooked: false, notes: '' };
const WEEKDAYS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

function cleanName(name) { const compact = name.replace(/\s+/g, ''); return compact ? compact.charAt(0).toUpperCase() + compact.slice(1) : ''; }
function nameKey(name) { return cleanName(name).toLowerCase(); }
function todayString() { return new Date().toISOString().slice(0, 10); }
function formatDate(date) { return date ? new Intl.DateTimeFormat(undefined, { weekday: 'short', day: 'numeric', month: 'short', year: 'numeric' }).format(new Date(`${date}T00:00:00`)) : 'Date not set'; }
function formatShortDate(date) { return date ? new Intl.DateTimeFormat(undefined, { day: 'numeric', month: 'short' }).format(new Date(`${date}T00:00:00`)) : 'Date not set'; }
function formatMonth(date) { return new Intl.DateTimeFormat(undefined, { month: 'long', year: 'numeric' }).format(date); }
function calendarAttendeeLabel(count) { return `${count} going`; }
function capacityFor(session) { return Number(session.courts || 1) * Number(session.maxPlayersPerCourt || DEFAULT_MAX_PLAYERS_PER_COURT); }
function spotsFilledLabel(session) { return `${session.attendees.length} / ${capacityFor(session)} spots filled`; }
function isFull(session) { return session.attendees.length >= capacityFor(session); }
function interestNames(session) { return session.interested.map((person) => person.name).join(', '); }
function interestLine(session) { return session.interested.length ? `Waitlist: ${interestNames(session)}` : ''; }
function calendarInterestLine(session) { return session.interested.length ? `Waitlist: ${session.interested.length}` : ''; }
function rsvpStatus(session, currentUser) { const going = Boolean(currentUser && session.attendeeIds.includes(currentUser.id)); return { going, label: going ? 'You RSVPed' : "Not RSVP'd" }; }
function formatClock(totalMinutes) { const normalized = ((totalMinutes % 1440) + 1440) % 1440; const hours24 = Math.floor(normalized / 60); const minutes = normalized % 60; const period = hours24 >= 12 ? 'PM' : 'AM'; const hours12 = hours24 % 12 || 12; return `${hours12}:${String(minutes).padStart(2, '0')} ${period}`; }
function formatStartTime(startTime) { const [hours, minutes] = String(startTime || '').split(':').map(Number); if (!Number.isFinite(hours) || !Number.isFinite(minutes)) return startTime || 'Time'; const period = hours >= 12 ? 'PM' : 'AM'; const hours12 = hours % 12 || 12; return minutes === 0 ? `${hours12} ${period}` : `${hours12}:${String(minutes).padStart(2, '0')} ${period}`; }
function formatTimeRange(startTime, durationHours) { const [hours, minutes] = String(startTime || '').split(':').map(Number); if (!Number.isFinite(hours) || !Number.isFinite(minutes)) return startTime || 'Time not set'; const startMinutes = hours * 60 + minutes; const endMinutes = startMinutes + Math.round(Number(durationHours || 0) * 60); return `${formatClock(startMinutes)} - ${formatClock(endMinutes)}`; }
function formatMoney(value) { return new Intl.NumberFormat(undefined, { style: 'currency', currency: 'AUD', maximumFractionDigits: value % 1 === 0 ? 0 : 2 }).format(Number.isFinite(value) ? value : 0); }
function knownAddress(location) { return LOCATION_DETAILS[location] || ''; }
function mapsLink(address) { return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(address)}`; }
function sessionPath(sessionId) { return `#/sessions/${sessionId}`; }
function editPath(sessionId) { return `#/sessions/${sessionId}/edit`; }
function goTo(hash) { window.location.hash = hash; }
function dateKeyFromDate(date) { return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`; }
function buildCalendarDays(monthDate) { const first = new Date(monthDate.getFullYear(), monthDate.getMonth(), 1); const daysInMonth = new Date(monthDate.getFullYear(), monthDate.getMonth() + 1, 0).getDate(); const mondayOffset = (first.getDay() + 6) % 7; return Array.from({ length: mondayOffset + daysInMonth }, (_, index) => index < mondayOffset ? null : new Date(monthDate.getFullYear(), monthDate.getMonth(), index - mondayOffset + 1)); }
function parseRoute() { const parts = window.location.hash.replace(/^#\/?/, '').split('/').filter(Boolean); if (parts[0] === 'create') return { page: 'create', sessionId: null }; if (parts[0] === 'sessions' && parts[1] && parts[2] === 'edit') return { page: 'edit', sessionId: parts[1] }; if (parts[0] === 'sessions' && parts[1]) return { page: 'details', sessionId: parts[1] }; return { page: 'home', sessionId: null }; }
function useHashRoute() { const [route, setRoute] = useState(parseRoute); useEffect(() => { function handleHashChange() { setRoute(parseRoute()); } window.addEventListener('hashchange', handleHashChange); return () => window.removeEventListener('hashchange', handleHashChange); }, []); return route; }

async function api(path, options = {}) {
  const response = await fetch(`${SUPABASE_URL}/rest/v1/${path}`, { ...options, headers: { apikey: SUPABASE_KEY, Authorization: `Bearer ${SUPABASE_KEY}`, 'Content-Type': 'application/json', ...(options.headers || {}) } });
  const text = await response.text();
  const data = text ? JSON.parse(text) : null;
  if (!response.ok) {
    const detail = data?.message || data?.hint || '';
    if (detail.includes('session_interest')) throw new Error('Supabase needs the latest waitlist update. Run supabase-schema.sql again, then refresh.');
    if (detail.includes('max_players_per_court')) throw new Error('Supabase needs the latest RSVP capacity update. Run supabase-schema.sql again, then refresh.');
    if (detail.includes('added_by')) throw new Error('Supabase needs the latest RSVP update. Run supabase-schema.sql again, then refresh.');
    if (detail.includes('session_address') || detail.includes('court_numbers') || detail.includes('is_booked')) throw new Error('Supabase needs the latest session update. Run supabase-schema.sql again, then refresh.');
    if (detail.includes('schema cache') || detail.includes('does not exist') || detail.includes('Could not find the table')) throw new Error('Database tables are not set up yet. Run supabase-schema.sql in Supabase, then refresh.');
    throw new Error(detail || 'Supabase request failed.');
  }
  return data;
}

function mapSession(row, members, attendeeRows, interestRows) {
  const creator = members.find((member) => member.id === row.creator_id);
  const sessionAttendees = attendeeRows.filter((item) => item.session_id === row.id);
  const sessionInterest = interestRows.filter((item) => item.session_id === row.id);
  const location = row.location || '';
  const maxPlayersPerCourt = Number(row.max_players_per_court || DEFAULT_MAX_PLAYERS_PER_COURT);
  return {
    id: row.id,
    title: row.title || '',
    date: row.session_date,
    startTime: String(row.start_time || '').slice(0, 5),
    durationHours: Number(row.duration_hours),
    location,
    address: row.session_address || knownAddress(location),
    courts: Number(row.courts),
    courtNumbers: row.court_numbers || '',
    maxPlayersPerCourt,
    costPerCourtHour: Number(row.cost_per_court_hour),
    isBooked: Boolean(row.is_booked),
    notes: row.notes || '',
    creatorId: row.creator_id,
    creatorName: creator?.name || 'Unknown',
    attendeeIds: sessionAttendees.map((item) => item.user_id),
    interestedIds: sessionInterest.map((item) => item.user_id),
    attendees: sessionAttendees.map((item) => { const member = members.find((person) => person.id === item.user_id); return member ? { ...member, addedById: item.added_by || null } : null; }).filter(Boolean).sort((a, b) => a.name.localeCompare(b.name)),
    interested: sessionInterest.map((item) => members.find((person) => person.id === item.user_id)).filter(Boolean).sort((a, b) => a.name.localeCompare(b.name))
  };
}

function PickleIcon() { return <span className="pickle-icon" aria-hidden="true"><span /><span /><span /></span>; }
function CreatorCredit() { return <p className="creator-credit">Created by Cousin Liam</p>; }
function LoginScreen({ onLogin, isBusy, setupError }) { const [name, setName] = useState(''); const [error, setError] = useState(''); function submit(event) { event.preventDefault(); const value = cleanName(name); if (value.length < 2) { setError('Please enter your first name.'); return; } onLogin(value).catch((loginError) => setError(loginError.message)); } return <main className="login-shell"><section className="login-panel" aria-labelledby="login-title"><div className="login-art" aria-hidden="true"><div className="brand-mark"><PickleIcon /></div><div className="court-mark court-one" /><div className="court-mark court-two" /><div className="ball-mark" /></div><div className="login-content"><p className="eyebrow">Family sessions</p><h1 id="login-title">Pickleball Planner</h1>{setupError ? <p className="setup-error">{setupError}</p> : null}<form onSubmit={submit} className="login-form"><label htmlFor="login-name">Login</label><p className="field-hint">Please login by entering your first name with a capital letter.</p><input id="login-name" value={name} onChange={(event) => { setName(event.target.value); setError(''); }} placeholder="e.g. Liam" autoComplete="given-name" />{error ? <p className="form-error">{error}</p> : null}<button className="primary-button" type="submit" disabled={isBusy}>{isBusy ? 'Logging in...' : 'Log In'}</button></form><CreatorCredit /></div></section></main>; }
function PageHeader({ title, subtitle, action }) { return <section className="page-heading"><div><button className="text-button" type="button" onClick={() => goTo('#/')}>Back to sessions</button><h2>{title}</h2>{subtitle ? <p>{subtitle}</p> : null}</div>{action}</section>; }

function SessionForm({ initialSession, onSubmit, onCancel, submitLabel, isBusy }) {
  const [session, setSession] = useState(initialSession ?? blankSession);
  const selectedLocation = LOCATION_OPTIONS.includes(session.location) ? session.location : (session.location ? 'custom' : '');
  const update = (field, value) => setSession((current) => ({ ...current, [field]: value }));
  function chooseLocation(value) { setSession((current) => value === 'custom' ? { ...current, location: '', address: '' } : { ...current, location: value, address: knownAddress(value) }); }
  function submit(event) { event.preventDefault(); const notes = session.notes.trim() || DEFAULT_NOTES; const location = session.location.trim(); const address = (session.address.trim() || knownAddress(location)); onSubmit({ ...session, title: session.title.trim(), location, address, courtNumbers: session.courtNumbers.trim(), isBooked: Boolean(session.isBooked), notes, durationHours: Number(session.durationHours), courts: Number(session.courts), maxPlayersPerCourt: Number(session.maxPlayersPerCourt || DEFAULT_MAX_PLAYERS_PER_COURT), costPerCourtHour: Number(session.costPerCourtHour) }); }
  return <form className="session-form" onSubmit={submit}><div className="field full"><label htmlFor="session-title">Session title <span>optional</span></label><input id="session-title" value={session.title} onChange={(e) => update('title', e.target.value)} placeholder="Saturday morning hit" /></div><div className="field"><label htmlFor="session-date">Date</label><input id="session-date" type="date" value={session.date} onChange={(e) => update('date', e.target.value)} required /></div><div className="field"><label htmlFor="session-time">Start time</label><input id="session-time" type="time" value={session.startTime} onChange={(e) => update('startTime', e.target.value)} required /></div><div className="field"><label htmlFor="session-duration">Duration</label><select id="session-duration" value={session.durationHours} onChange={(e) => update('durationHours', e.target.value)}><option value="0.5">30 minutes</option><option value="1">1 hour</option><option value="1.5">1.5 hours</option><option value="2">2 hours</option><option value="2.5">2.5 hours</option><option value="3">3 hours</option></select></div><div className="field"><label htmlFor="session-location-choice">Location</label><select id="session-location-choice" value={selectedLocation} onChange={(e) => chooseLocation(e.target.value)} required><option value="" disabled>Choose a location</option>{LOCATION_OPTIONS.map((location) => <option key={location} value={location}>{location}</option>)}<option value="custom">Add a new location</option></select>{selectedLocation === 'custom' ? <input className="location-custom" value={session.location} onChange={(e) => update('location', e.target.value)} placeholder="Enter location name" required /> : null}</div><div className="field"><label htmlFor="session-address">Address</label><input id="session-address" value={session.address} onChange={(e) => update('address', e.target.value)} placeholder="Enter the address from Maps" autoComplete="street-address" required /></div><div className="field"><label htmlFor="session-courts">Number of courts booked</label><input id="session-courts" type="number" min="1" step="1" value={session.courts} onChange={(e) => update('courts', e.target.value)} required /></div><div className="field"><label htmlFor="session-max-players">Maximum players per court</label><p className="field-hint">This helps limit RSVPs so the number of attendees fits the courts booked.</p><input id="session-max-players" type="number" min="1" step="1" value={session.maxPlayersPerCourt ?? DEFAULT_MAX_PLAYERS_PER_COURT} onChange={(e) => update('maxPlayersPerCourt', e.target.value)} required /></div><div className="field"><label htmlFor="session-court-numbers">Court number(s) <span>optional</span></label><input id="session-court-numbers" value={session.courtNumbers} onChange={(e) => update('courtNumbers', e.target.value)} placeholder="e.g. Court 3, Court 4" /></div><div className="field"><label htmlFor="session-cost">Cost per court per hour</label><input id="session-cost" type="number" min="0" step="0.01" value={session.costPerCourtHour} onChange={(e) => update('costPerCourtHour', e.target.value)} required /></div><label className={session.isBooked ? 'booking-check full checked' : 'booking-check full'} htmlFor="session-booked"><input id="session-booked" type="checkbox" checked={Boolean(session.isBooked)} onChange={(e) => update('isBooked', e.target.checked)} /><span className="booking-check-text"><strong>Session booked</strong><small>Tick this once the court booking is confirmed.</small></span></label><div className="field full"><label htmlFor="session-notes">Notes <span>optional</span></label><textarea id="session-notes" value={session.notes} onChange={(e) => update('notes', e.target.value)} placeholder={DEFAULT_NOTES} /></div><div className="form-actions full">{onCancel ? <button className="secondary-button" type="button" onClick={onCancel}>Cancel</button> : null}<button className="primary-button" type="submit" disabled={isBusy}>{isBusy ? 'Saving...' : submitLabel}</button></div></form>;
}

function RsvpBadge({ session, currentUser }) { const status = rsvpStatus(session, currentUser); return <span className={status.going ? 'rsvp-status going' : 'rsvp-status not-going'}>{status.label}</span>; }
function SummaryButton({ session, currentUser, onOpen }) { const waitlist = interestLine(session); return <button className={rsvpStatus(session, currentUser).going ? 'summary-row rsvp-yes' : 'summary-row rsvp-no'} type="button" onClick={() => onOpen(session.id)}><span className="summary-date">{formatShortDate(session.date)}</span><span className="summary-main"><strong>{session.title || 'Pickleball Session'}</strong><small>{formatTimeRange(session.startTime, session.durationHours)} at {session.location}</small><small>{spotsFilledLabel(session)} - Created by {session.creatorName}</small>{waitlist ? <small className="summary-interest">{waitlist}</small> : null}</span><span className="summary-badges"><RsvpBadge session={session} currentUser={currentUser} /><span className={session.isBooked ? 'booking-status booked' : 'booking-status pending'}>{session.isBooked ? 'Booked' : 'Not booked'}</span></span></button>; }
function CalendarSection({ sessions, currentUser, onOpen }) { const bookedSessions = sessions.filter((session) => session.isBooked); const sessionsByDate = bookedSessions.reduce((grouped, session) => { grouped[session.date] = [...(grouped[session.date] || []), session]; return grouped; }, {}); const monthDates = bookedSessions.length ? Array.from(new Set(bookedSessions.map((session) => session.date.slice(0, 7)))).map((month) => new Date(`${month}-01T00:00:00`)) : [new Date()]; return <section className="overview-panel calendar-panel"><div className="section-heading"><h2>Calendar</h2><p>Booked sessions</p></div><div className="calendar-months">{monthDates.map((monthDate) => { const calendarDays = buildCalendarDays(monthDate); return <div className="calendar-month" key={formatMonth(monthDate)}><h3 className="calendar-month-title">{formatMonth(monthDate)}</h3><div className="calendar-weekdays">{WEEKDAYS.map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-grid">{calendarDays.map((day, index) => { const key = day ? dateKeyFromDate(day) : `${formatMonth(monthDate)}-blank-${index}`; const daySessions = day ? sessionsByDate[key] || [] : []; return <div className={daySessions.length ? 'calendar-day has-booking' : 'calendar-day'} key={key}>{day ? <span className="calendar-day-number">{day.getDate()}</span> : null}{daySessions.map((session) => { const status = rsvpStatus(session, currentUser); const waitlist = calendarInterestLine(session); return <button className={status.going ? 'calendar-booking rsvp-yes' : 'calendar-booking rsvp-no'} type="button" key={session.id} onClick={() => onOpen(session.id)}><strong>{formatStartTime(session.startTime)}</strong><span>{calendarAttendeeLabel(session.attendees.length)}</span>{waitlist ? <span className="calendar-interest">{waitlist}</span> : null}</button>; })}</div>; })}</div></div>; })}</div>{bookedSessions.length ? null : <p className="empty-text calendar-empty">No booked sessions yet.</p>}</section>; }
function UpcomingSection({ sessions, currentUser, onOpen }) { return <section className="overview-panel wide-panel"><div className="section-heading"><h2>Upcoming</h2><p>Tap a session to open its page.</p></div>{sessions.length ? <div className="summary-list">{sessions.map((session) => <SummaryButton key={session.id} session={session} currentUser={currentUser} onOpen={onOpen} />)}</div> : <p className="empty-text">No upcoming sessions yet.</p>}</section>; }
function MyBookingsSection({ sessions, currentUser, onOpen, onEdit, onDelete, isBusy }) { const mySessions = sessions.filter((session) => session.creatorId === currentUser.id); return <section className="overview-panel"><div className="section-heading"><h2>My Bookings</h2><p>Edit sessions you created.</p></div>{mySessions.length ? <div className="my-bookings-list">{mySessions.map((session) => <div className="my-booking" key={session.id}><button className="my-booking-main" type="button" onClick={() => onOpen(session.id)}><strong>{session.title || 'Pickleball Session'}</strong><span>{formatDate(session.date)} at {formatTimeRange(session.startTime, session.durationHours)}</span></button><div className="my-booking-actions"><button className="secondary-button compact" type="button" onClick={() => onEdit(session.id)}>Edit</button><button className="danger-button compact" type="button" onClick={() => onDelete(session.id)} disabled={isBusy}>Delete</button></div></div>)}</div> : <p className="empty-text">You have not created any sessions yet.</p>}</section>; }

function RsvpPanel({ session, users, currentUser, onAddAttendees, onRemoveAttendee, onShowInterest, onRemoveInterest, isBusy }) {
  const [selectedUserIds, setSelectedUserIds] = useState([]);
  const attendeeSet = useMemo(() => new Set(session.attendeeIds), [session.attendeeIds]);
  const familyChoices = users.filter((user) => !attendeeSet.has(user.id));
  const isCurrentUserAttending = attendeeSet.has(currentUser.id);
  const isCurrentUserInterested = session.interestedIds.includes(currentUser.id);
  const capacity = capacityFor(session);
  const remainingSpots = Math.max(0, capacity - session.attendees.length);
  const sessionFull = remainingSpots === 0;
  function toggleUser(userId) { setSelectedUserIds((current) => current.includes(userId) ? current.filter((id) => id !== userId) : [...current, userId]); }
  function addAttendees(ids) { onAddAttendees(session.id, ids).then(() => setSelectedUserIds([])); }
  return <div className="rsvp-panel"><div className="rsvp-help"><strong>RSVP to this session</strong><span>{spotsFilledLabel(session)}. {sessionFull ? 'Confirmed RSVP spots are full.' : `${remainingSpots} confirmed spot${remainingSpots === 1 ? '' : 's'} left.`}</span></div>{sessionFull ? <div className="interest-callout"><p>{isCurrentUserAttending ? 'This session is full and you already have a confirmed RSVP spot.' : "RSVP spots are currently full. Show interest to let the organiser know you'd like to join if more courts are booked or if someone cancels their RSVP."}</p>{isCurrentUserAttending ? <button className="secondary-button compact" type="button" onClick={() => onRemoveAttendee(session.id, currentUser.id)} disabled={isBusy}>Not Attending</button> : <button className="primary-button compact" type="button" onClick={() => onShowInterest(session.id)} disabled={isBusy || isCurrentUserInterested}>{isCurrentUserInterested ? 'Interest Shown' : 'Show Interest'}</button>}{isCurrentUserInterested && !isCurrentUserAttending ? <button className="secondary-button compact" type="button" onClick={() => onRemoveInterest(session.id)} disabled={isBusy}>Remove Interest</button> : null}</div> : <div className="button-row"><button className="primary-button compact" type="button" onClick={() => addAttendees([currentUser.id])} disabled={isBusy || isCurrentUserAttending}>I'm Attending</button><button className="secondary-button compact" type="button" onClick={() => onRemoveAttendee(session.id, currentUser.id)} disabled={isBusy || !isCurrentUserAttending}>Not Attending</button></div>}<div className="family-rsvp"><div className="family-rsvp-header"><div><h4>RSVP for Family</h4><p>{sessionFull ? 'Confirmed spots are full. Ask the organiser to add more courts, then RSVP again.' : `Tick the people you are bringing. You can add up to ${remainingSpots} more.`}</p></div><button className="secondary-button compact" type="button" onClick={() => addAttendees(selectedUserIds)} disabled={isBusy || sessionFull || selectedUserIds.length === 0 || selectedUserIds.length > remainingSpots}>Add {selectedUserIds.length || ''} Selected</button></div>{!sessionFull && familyChoices.length ? <div className="family-checklist">{familyChoices.map((user) => <label className={selectedUserIds.includes(user.id) ? 'family-check selected' : 'family-check'} key={user.id}><input type="checkbox" checked={selectedUserIds.includes(user.id)} onChange={() => toggleUser(user.id)} /> <span>{user.name}</span></label>)}</div> : null}</div></div>;
}
function SessionCard({ session, users, currentUser, onAddAttendees, onRemoveAttendee, onShowInterest, onRemoveInterest, onEdit, onDelete, isBusy }) {
  const canManage = session.creatorId === currentUser.id;
  const capacity = capacityFor(session);
  return <article className="session-card"><div className="session-card-header"><div><p className="date-line">{formatDate(session.date)} at {formatTimeRange(session.startTime, session.durationHours)}</p><div className="session-title-row"><h3>{session.title || 'Pickleball Session'}</h3><span className={session.isBooked ? 'booking-status booked' : 'booking-status pending'}>{session.isBooked ? 'Booked' : 'Not booked'}</span></div></div>{canManage ? <div className="card-actions"><button className="secondary-button compact" type="button" onClick={() => onEdit(session.id)}>Edit Session</button><button className="danger-button compact" type="button" onClick={() => onDelete(session.id)} disabled={isBusy}>Delete Session</button></div> : null}</div><dl className="session-details"><div><dt>Booking status</dt><dd>{session.isBooked ? 'Booked' : 'Not booked'}</dd></div><div><dt>RSVP capacity</dt><dd>{session.attendees.length} / {capacity} spots filled</dd></div><div><dt>Duration</dt><dd>{session.durationHours} hour{session.durationHours === 1 ? '' : 's'}</dd></div><div><dt>Location</dt><dd>{session.location}</dd></div><div><dt>Address</dt><dd>{session.address ? <a className="map-link" href={mapsLink(session.address)} target="_blank" rel="noreferrer">{session.address}</a> : 'Not added'}</dd></div><div><dt>Courts booked</dt><dd>{session.courts}</dd></div><div><dt>Max players per court</dt><dd>{session.maxPlayersPerCourt}</dd></div>{session.courtNumbers ? <div><dt>Court number(s)</dt><dd>{session.courtNumbers}</dd></div> : null}<div><dt>Court cost</dt><dd>{formatMoney(session.costPerCourtHour)} / hour</dd></div><div><dt>Created by</dt><dd>{session.creatorName}</dd></div></dl>{session.notes ? <p className="session-notes">{session.notes}</p> : null}<section className="attendees" aria-label="Confirmed attendees"><h4>Confirmed attendees</h4>{session.attendees.length ? <div className="attendee-list">{session.attendees.map((user) => { const canRemove = user.id === currentUser.id || user.addedById === currentUser.id; return <span className="attendee-pill" key={user.id}>{user.name}{canRemove ? <button type="button" aria-label={`Remove ${user.name}`} onClick={() => onRemoveAttendee(session.id, user.id)} disabled={isBusy}>x</button> : null}</span>; })}</div> : <p className="empty-text">No attendees confirmed yet</p>}<p className="attendee-note">You can remove yourself, or family members you added.</p></section><section className="attendees waitlist" aria-label="Interested waitlist"><h4>Interested / Waitlist</h4>{session.interested.length ? <div className="attendee-list">{session.interested.map((user) => <span className="attendee-pill waitlist-pill" key={user.id}>{user.name}{user.id === currentUser.id ? <button type="button" aria-label={`Remove ${user.name} from waitlist`} onClick={() => onRemoveInterest(session.id)} disabled={isBusy}>x</button> : null}</span>)}</div> : <p className="empty-text">No one has shown interest yet</p>}</section><RsvpPanel session={session} users={users} currentUser={currentUser} onAddAttendees={onAddAttendees} onRemoveAttendee={onRemoveAttendee} onShowInterest={onShowInterest} onRemoveInterest={onRemoveInterest} isBusy={isBusy} /></article>;
}

export default function App() {
  const [users, setUsers] = useState([]); const [sessions, setSessions] = useState([]); const [currentUser, setCurrentUser] = useState(() => { try { return JSON.parse(localStorage.getItem(CURRENT_USER_KEY)); } catch { return null; } }); const [isBusy, setIsBusy] = useState(false); const [message, setMessage] = useState(''); const [setupError, setSetupError] = useState(''); const route = useHashRoute(); const routeSession = sessions.find((session) => session.id === route.sessionId) || null;
  async function loadData() { const [members, sessionRows, attendeeRows, interestRows] = await Promise.all([api('family_members?select=id,name&order=name.asc'), api(`sessions?select=id,title,session_date,start_time,duration_hours,location,session_address,courts,court_numbers,max_players_per_court,cost_per_court_hour,is_booked,notes,creator_id,created_at&session_date=gte.${todayString()}&order=session_date.asc,start_time.asc`), api('session_attendees?select=session_id,user_id,added_by'), api('session_interest?select=session_id,user_id')]); setUsers(members); setSessions(sessionRows.map((session) => mapSession(session, members, attendeeRows, interestRows))); setSetupError(''); }
  useEffect(() => { if (currentUser) loadData().catch((error) => setSetupError(error.message)); }, [currentUser]);
  async function login(name) { setIsBusy(true); setMessage(''); try { const key = nameKey(name); const existing = await api(`family_members?select=id,name&normalized_name=eq.${encodeURIComponent(key)}&limit=1`); let user = existing[0]; if (!user) { try { const created = await api('family_members', { method: 'POST', headers: { Prefer: 'return=representation' }, body: JSON.stringify({ name, normalized_name: key }) }); user = created[0]; } catch { const matchingUsers = await api(`family_members?select=id,name&normalized_name=eq.${encodeURIComponent(key)}&limit=1`); user = matchingUsers[0]; } } setCurrentUser(user); localStorage.setItem(CURRENT_USER_KEY, JSON.stringify(user)); await loadData(); } catch (error) { setSetupError(error.message); throw error; } finally { setIsBusy(false); } }
  function logout() { setCurrentUser(null); localStorage.removeItem(CURRENT_USER_KEY); goTo('#/'); }
  async function saveSession(session) { setIsBusy(true); setMessage(''); const payload = { title: session.title || null, session_date: session.date, start_time: session.startTime, duration_hours: session.durationHours, location: session.location, session_address: session.address || null, courts: session.courts, court_numbers: session.courtNumbers || null, max_players_per_court: session.maxPlayersPerCourt || DEFAULT_MAX_PLAYERS_PER_COURT, cost_per_court_hour: session.costPerCourtHour, is_booked: Boolean(session.isBooked), notes: session.notes || null }; try { if (session.id) { await api(`sessions?id=eq.${session.id}`, { method: 'PATCH', headers: { Prefer: 'return=minimal' }, body: JSON.stringify(payload) }); setMessage('Session updated.'); await loadData(); goTo(sessionPath(session.id)); } else { const created = await api('sessions', { method: 'POST', headers: { Prefer: 'return=representation' }, body: JSON.stringify({ ...payload, creator_id: currentUser.id }) }); setMessage('Session created.'); await loadData(); goTo(sessionPath(created?.[0]?.id || '')); } } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  async function deleteSession(sessionId) { const session = sessions.find((item) => item.id === sessionId); if (!window.confirm(`Delete ${session?.title || 'this pickleball session'}? This cannot be undone.`)) return; setIsBusy(true); setMessage(''); try { await api(`sessions?id=eq.${sessionId}`, { method: 'DELETE', headers: { Prefer: 'return=minimal' } }); setMessage('Session deleted.'); await loadData(); goTo('#/'); } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  async function addAttendees(sessionId, attendeeIds) { setIsBusy(true); setMessage(''); try { const session = sessions.find((item) => item.id === sessionId); const newIds = [...new Set(attendeeIds)].filter((userId) => !session?.attendeeIds.includes(userId)); const remaining = session ? capacityFor(session) - session.attendees.length : 0; if (!session || newIds.length === 0) return; if (newIds.length > remaining) { setMessage(`Only ${remaining} confirmed spot${remaining === 1 ? '' : 's'} left.`); return; } const rows = newIds.map((userId) => ({ session_id: sessionId, user_id: userId, added_by: currentUser.id })); await api('session_attendees?on_conflict=session_id,user_id', { method: 'POST', headers: { Prefer: 'resolution=ignore-duplicates,return=minimal' }, body: JSON.stringify(rows) }); await api(`session_interest?session_id=eq.${sessionId}&user_id=in.(${newIds.join(',')})`, { method: 'DELETE', headers: { Prefer: 'return=minimal' } }); await loadData(); } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  async function removeAttendee(sessionId, userId) { setIsBusy(true); setMessage(''); try { const extraGuard = userId === currentUser.id ? '' : `&added_by=eq.${currentUser.id}`; await api(`session_attendees?session_id=eq.${sessionId}&user_id=eq.${userId}${extraGuard}`, { method: 'DELETE', headers: { Prefer: 'return=minimal' } }); await loadData(); } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  async function showInterest(sessionId) { setIsBusy(true); setMessage(''); try { await api('session_interest?on_conflict=session_id,user_id', { method: 'POST', headers: { Prefer: 'resolution=ignore-duplicates,return=minimal' }, body: JSON.stringify([{ session_id: sessionId, user_id: currentUser.id }]) }); await loadData(); } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  async function removeInterest(sessionId) { setIsBusy(true); setMessage(''); try { await api(`session_interest?session_id=eq.${sessionId}&user_id=eq.${currentUser.id}`, { method: 'DELETE', headers: { Prefer: 'return=minimal' } }); await loadData(); } catch (error) { setMessage(error.message); } finally { setIsBusy(false); } }
  function renderPage() { if (route.page === 'create') return <div className="page-stack"><PageHeader title="Create Session" subtitle="Add the booking details, then family can RSVP from the session page." /><section className="form-panel"><SessionForm initialSession={blankSession} onSubmit={saveSession} onCancel={() => goTo('#/')} submitLabel="Create Session" isBusy={isBusy} /></section></div>; if (route.page === 'edit') { if (!routeSession) return <div className="empty-state"><h3>Session not found</h3><p>This booking may have been deleted.</p><button className="primary-button" type="button" onClick={() => goTo('#/')}>Back to sessions</button></div>; if (routeSession.creatorId !== currentUser.id) return <div className="empty-state"><h3>Only the creator can edit this session</h3><p>You can still view the booking details and RSVP.</p><button className="primary-button" type="button" onClick={() => goTo(sessionPath(routeSession.id))}>View Session</button></div>; return <div className="page-stack"><PageHeader title="Edit Session" subtitle={`${formatDate(routeSession.date)} at ${formatTimeRange(routeSession.startTime, routeSession.durationHours)}`} /><section className="form-panel"><SessionForm key={routeSession.id} initialSession={routeSession} onSubmit={saveSession} onCancel={() => goTo(sessionPath(routeSession.id))} submitLabel="Save Changes" isBusy={isBusy} /></section></div>; } if (route.page === 'details') { if (!routeSession) return <div className="empty-state"><h3>Session not found</h3><p>This booking may have been deleted.</p><button className="primary-button" type="button" onClick={() => goTo('#/')}>Back to sessions</button></div>; return <div className="page-stack"><PageHeader title="Session Details" subtitle="View the booking, RSVP, or add family members." action={routeSession.creatorId === currentUser.id ? <button className="secondary-button" type="button" onClick={() => goTo(editPath(routeSession.id))}>Edit Session</button> : null} /><section className="session-list"><SessionCard key={routeSession.id} session={routeSession} users={users} currentUser={currentUser} onAddAttendees={addAttendees} onRemoveAttendee={removeAttendee} onShowInterest={showInterest} onRemoveInterest={removeInterest} onEdit={(id) => goTo(editPath(id))} onDelete={deleteSession} isBusy={isBusy} /></section></div>; } return <><section className="toolbar"><div><h2>Session Planner</h2><p>{sessions.length ? `${sessions.length} upcoming session${sessions.length === 1 ? '' : 's'}.` : 'No upcoming sessions yet.'}</p></div><button className="primary-button" type="button" onClick={() => goTo('#/create')}>Create Session</button></section><div className="planner-grid"><CalendarSection sessions={sessions} currentUser={currentUser} onOpen={(id) => goTo(sessionPath(id))} /><UpcomingSection sessions={sessions} currentUser={currentUser} onOpen={(id) => goTo(sessionPath(id))} /><MyBookingsSection sessions={sessions} currentUser={currentUser} onOpen={(id) => goTo(sessionPath(id))} onEdit={(id) => goTo(editPath(id))} onDelete={deleteSession} isBusy={isBusy} /></div></>; }
  if (!currentUser) return <LoginScreen onLogin={login} isBusy={isBusy} setupError={setupError} />;
  return <div className="app-shell"><header className="app-header"><button className="app-title-button" type="button" onClick={() => goTo('#/')}><h1>Pickleball Planner</h1></button><div className="user-box"><span>Logged in as <strong>{currentUser.name}</strong></span><button className="secondary-button compact" type="button" onClick={logout}>Logout</button></div></header><main>{setupError ? <p className="setup-error">{setupError}</p> : null}{message ? <p className="status-message">{message}</p> : null}{renderPage()}</main><CreatorCredit /></div>;
}
